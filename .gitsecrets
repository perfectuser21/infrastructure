# Git-secrets patterns to prevent secret leaks
# https://github.com/awslabs/git-secrets

# AWS Keys
[A-Z0-9]{20}
[A-Za-z0-9/+=]{40}

# GitHub Tokens
ghp_[A-Za-z0-9]{36}
gho_[A-Za-z0-9]{36}
github_pat_[A-Za-z0-9]{22}_[A-Za-z0-9]{59}

# Generic API Keys
api[_-]?key[_-]?=[\w-]{20,}
secret[_-]?key[_-]?=[\w-]{20,}

# Private Keys
-----BEGIN (RSA|DSA|EC|OPENSSH) PRIVATE KEY-----
-----BEGIN PGP PRIVATE KEY BLOCK-----

# Common Passwords (should never be hardcoded)
password\s*=\s*["'][^"']{8,}["']
passwd\s*=\s*["'][^"']{8,}["']
pwd\s*=\s*["'][^"']{8,}["']

# Database URLs with credentials
(postgres|postgresql|mysql|mongodb|redis)://[^:]+:[^@]+@

# Slack Tokens
xox[baprs]-[0-9]{12}-[0-9]{12}-[a-zA-Z0-9]{24}

# Generic Secrets
[Ss][Ee][Cc][Rr][Ee][Tt].*=.*[A-Za-z0-9+/]{20,}