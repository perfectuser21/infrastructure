---
id: mac-mini-device
version: 1.0.0
created: 2026-02-10
changelog:
  - 1.0.0: 初始版本 - Mac mini 设备文档
---

# Mac mini (西安公司)

## 📋 基本信息

| 项目 | 信息 |
|------|------|
| **位置** | 西安公司 |
| **型号** | Mac mini 4 |
| **Tailscale IP** | 100.86.57.69 |
| **局域网 IP** | 192.168.x.x (待确认) |
| **系统** | macOS |
| **用途** | 开发设备、内容生成 |

---

## 🔌 连接方式

### 通过 Tailscale

```bash
# SSH 连接
ssh <user>@100.86.57.69

# 或使用别名
ssh mac-mini

# 从美国 VPS 连接
ssh mac-mini  # 配置在 ~/.ssh/config
```

### 通过局域网（在公司）

```bash
# 直接连接
ssh <user>@192.168.x.x
```

---

## 🌐 网络配置

### Tailscale

- **Tailscale IP**: 100.86.57.69
- **状态**: ✅ 在线
- **Exit Node**: 根据需要启用
- **问题**: 使用 exit node 时可能很卡，丢包 50%

### VPN

**推荐配置**: WARP（如需翻墙）

```bash
# 安装 WARP
# 访问: https://1.1.1.1/

# 连接
warp-cli connect

# 断开
warp-cli disconnect
```

**不推荐**: Tailscale exit node（在 Mac mini 上表现不佳）

---

## 💻 用途

### 1. 开发环境

- 本地开发和测试
- 前端构建
- 视频处理

### 2. 文件共享

- 与 NAS 共享文件
- 与 Node PC 共享文件

### 3. 内容生成

- 视频渲染
- 图像处理
- AI 模型运行（如有）

---

## 📂 常用目录

| 目录 | 用途 |
|------|------|
| `~/Projects/` | 开发项目 |
| `~/Downloads/` | 下载目录 |
| `~/nas/` | NAS 挂载点（待配置） |

---

## 🔧 NAS 挂载配置

### 挂载 NAS

```bash
# 创建挂载点
mkdir -p ~/nas

# 挂载 NAS（通过 Tailscale IP）
mount -t smbfs //perfect21:<密码>@<nas-tailscale-ip>/shared ~/nas

# 或使用局域网 IP
mount -t smbfs //perfect21:<密码>@192.168.x.x/shared ~/nas
```

### 自动挂载

创建脚本 `~/mount-nas.sh`：

```bash
#!/bin/bash
mkdir -p ~/nas
mount -t smbfs //perfect21:<密码>@<nas-ip>/shared ~/nas
```

添加到启动项：
```
系统偏好设置 → 用户与群组 → 登录项 → 添加 mount-nas.sh
```

---

## 🛠️ 常用操作

### 系统信息

```bash
# 查看系统版本
sw_vers

# 查看硬件信息
system_profiler SPHardwareDataType

# 查看磁盘空间
df -h

# 查看内存使用
vm_stat
```

### 网络诊断

```bash
# 查看 Tailscale 状态
tailscale status

# 测试网络连通性
ping -c 3 100.71.32.28  # 美国 VPS

# 测试 NAS 连接
ping -c 3 <nas-ip>
```

---

## ⚠️ 已知问题

### 问题 1: Tailscale Exit Node 很卡

**症状**: 使用 Tailscale exit node 时，direct 连接丢包 50%

**临时方案**:
- 在 Mac mini 上装 WARP，用 WARP 翻墙
- 或接受慢速度（如不常用）

**待排查**:
- [ ] 检查 Mac mini 网络配置
- [ ] 测试 Mac mini → VPS 的网络质量
- [ ] 考虑换用 WARP 或其他 VPN

---

## 🔗 相关文档

- 网络拓扑: [../network/topology.md](../network/topology.md)
- NAS 配置: [../nas/setup.md](../nas/setup.md)
- VPN 配置: [../network/vpn.md](../network/vpn.md)
- Node PC: [node-pc.md](./node-pc.md)
